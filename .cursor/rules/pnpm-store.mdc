---
description: 禁止在仓库里创建 .pnpm-store，安装依赖必须用全局 pnpm store
alwaysApply: true
---

# pnpm store

本机全局缓存在 `~/Library/pnpm/store`。仓库根目录、子目录 都不要出现 `.pnpm-store`。

<!-- - 只在 `server/`、`client/`、`mcp-server/` 里跑 `pnpm add` / `pnpm install`，永远不要在仓库根目录装依赖。 -->
- 跑 pnpm 时必须申请 `all` 权限，让它写到全局 store。沙箱写不了 `~/Library/pnpm/store` 时，pnpm 会在项目里生成 `.pnpm-store`。
- 不要设置项目级 `store-dir`，不要 `pnpm install --store-dir .pnpm-store`。
- 如果又出现 `.pnpm-store`，删掉即可，不要提交。
